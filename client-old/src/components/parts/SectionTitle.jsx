import '../../style/parts/SectionTitle.css'

function SectionTitle({name}) {
    return (
        <>
            <h1 className='section-title'>{name}</h1>
        </>
    )
}
export default SectionTitle